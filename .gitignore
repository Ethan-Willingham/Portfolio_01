# Claude Code / AI assistant config
.claude/

# Research corpora (large, local working dirs; not part of the site)
.baby-research/
.ocean-research/

# One-off scripts
decode_b64.ps1

# Verification harness (dev-only)
sluice-verify.html

# OS junk
Thumbs.db
.DS_Store

# Debug screenshots (never commit these)
*-verify.png
*-smoke-*.png

# Temp commit-message files used by the agent
.git_commit_msg.tmp

# Raw audio source (large WAV masters). The shipped game uses the converted
# .m4a in assets/audio/; keep the WAV originals local, do not commit them.
assets/Sluice Audio/

# Raw ultra-high-res zoom sources (the 50 to 145MB die/wafer/machine masters).
# The shipped post uses the web-optimized derivatives in assets/images/zoom/;
# keep the masters local, do not commit them.
assets/images/zoom-src/

# The random-galaxy sky master (8.2MB jpg). The site ships the 3.4MB webp
# derivative (assets/images/nebula-bg.webp); keep the jpg local for re-encodes.
assets/images/nebula-bg.jpg

# --- Local working scratch (kept on disk, not part of the deployed site) ---
# Research corpora + dossiers (large; one working dir per post topic).
# Untracked for real on 2026-07-12: the early corpora were committed before this
# rule existed and were being served publicly, including in-copyright translation
# sources. Keep every research file local-only.
research/
MANAGING_PROFESSIONALS.md
# Old whole-site style experiments (orphaned demos; the only pages that ever
# loaded Google Fonts). Kept local for reference, off the deployed site.
style-family.html
style-linear.html
style-press.html
# Throwaway *-lab.html choosers that were never committed.
debate-lab.html
kit-cleanup-lab.html
opening-lab.html
search-lab.html
thumbs-lab.html
voice-lab.html
# Thumbnail-experiment staging.
assets/thumbs/_lab/
# Stale root copy of a post that now lives under archive/.
nothing-new-under-the-sun.html
assets/thumbs/nothing-new-under-the-sun.jpg
# Orphan single-file game build from the abandoned re-freeze (the kept game uses js/sluice.js).
js/grand-motherload.js
# Raw scratch captures.
mc_archive_raw.html
mc_fadedpage.txt
# Unused image sources that did not ship.
assets/photos/
assets/darwin/darwin-portrait-late.jpg
assets/darwin/origin-title-page-1859.jpg
assets/darwin/tree-of-life-diagram-1859.png
assets/meditation/*-src.jpg
assets/shop/soviet/masks/
